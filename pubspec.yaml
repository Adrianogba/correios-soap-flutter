name: correios_app
description: Simples exemplo de consumo da API SOAP dos correios. Desenvolvido por github.com/adrianogba.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  xml: ^3.4.1
  http: ^0.12.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
